language: python
sudo: false
python:
  - "3.6"
cache:
  yarn: true
  directories:
    - node_modules
    - $HOME/.cache/pip/wheels
env:
  global:
    - NODE_ENV=production
    - PIP_DISABLE_PIP_VERSION_CHECK=on
install:
  - npm install -g yarn@0.27.5
  - make
script:
  - py.test tests --cov zeus --cov-report term-missing
after_success:
  - codecov
